# The public GitHub handle used by the server-side repository fetcher.
GITHUB_USERNAME=Sumeet-basfore

# Optional: raises GitHub API limits. Never expose this with NEXT_PUBLIC_.
GITHUB_TOKEN=

# Optional: used for canonical metadata and sitemap generation.
NEXT_PUBLIC_SITE_URL=https://your-production-domain.example
